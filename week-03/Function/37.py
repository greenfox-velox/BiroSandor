numbers = [3, 4, 5, 6, 7]
# write a function that filters the odd numbers
# from a list and returns a new list consisting
# only the evens

def odd(input):
    for i in input
        if i % 2 != 0:
            return odd
        else:

odd(numbers)
